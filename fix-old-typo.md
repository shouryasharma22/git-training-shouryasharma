Commands-

git start fix-old-typo

git rebase -i HEAD^^

(fixed the first commit by using "edit")

(manually fixed the typo in the file using "nano")

git add file.txt

git rebase --continue

(fixed the typo in the commit message)